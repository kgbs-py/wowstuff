{
  "type": "module",
  "scripts": {
    "build": "browserify ./bundle.js > ./build/bundle.js",
    "push": "node download.js ; git add data ; git commit -a -m data ; git push origin gh-pages",
    "serve": "serve -l tcp://0.0.0.0:3000"
  },
  "dependencies": {
    "assert": "^2.0.0",
    "browserify": "^17.0.0",
    "ini": "^2.0.0",
    "needle": "^2.8.0",
    "serve": "^12.0.0"
  }
}
